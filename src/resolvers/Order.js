const Order = {
    // products(parent, args, { prisma }, info){

    //     console.log(parent)
    //     console.log(args)
    //     return prisma.query.products({
    //         where: {
    //             id: parent.products.id
    //         }
    //     }, info)
    // }
}

export default Order